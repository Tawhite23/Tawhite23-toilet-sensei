# セリフ文字起こし用（scripts/transcribe.py）
#
# yt-dlp は [default] 付きで入れること。
# YouTubeのJSチャレンジ(EJS)を解くためのスクリプト(yt-dlp-ejs)が同梱される。
# これが無いと "Requested format is not available" で失敗する。
# 加えて JSランタイム(Node 20+ または Deno)が PATH に必要。
yt-dlp[default]>=2026.6.0
faster-whisper>=1.0.3
pykakasi>=2.2.1

# PO Token プロバイダ（字幕が無く音声ダウンロードが必要な配信向け）。
# これが無いと字幕なし配信は "No video formats found!" で失敗することがある。
# 対になるサーバー本体(Node.js製)を別途 setup-pot.sh / setup-pot.ps1 で用意する。
bgutil-ytdlp-pot-provider>=1.3.1
